<%= form_for(@brasseries) do |f| %>
  <% if @brasseries.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@brasseries.errors.count, "error") %> prohibited this brasseries from being saved:</h2>

      <ul>
      <% @brasseries.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nom %><br>
    <%= f.text_field :nom %>
  </div>
  <div class="field">
    <%= f.label :desc %><br>
    <%= f.text_area :desc %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
